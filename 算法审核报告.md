# FileUniquefy 算法与文件操作审核报告

## 审核结论

重构后的算法满足“先只读扫描、明确展示计划、执行前逐项复核、目标永不覆盖、操作可审计”的安全目标。命令行版和图形界面版调用同一共享核心，不存在两套去重算法结果不一致的问题。

旧版的自动目录发现与统计、逐月份重复统计、详细计划、第一层扫描、文件名优先保留和返回主界面功能均已纳入回归检查；新增 GUI 同样展示目录统计和月份统计，没有以图形化为由省略原有信息。

工具执行的是“移动到配置目的地”，不是永久删除。当前默认目的地为 `E:\Down\weixin`。仍建议首次在少量月份副本上验证，并在确认目的地内容无误后再自行删除。

## 原实现发现的问题及处理

| 编号 | 原实现风险 | 影响 | 当前处理 |
|---|---|---|---|
| A-01 | 使用 MD5 判定内容相同 | MD5 已不适合作为高严谨性内容身份算法 | 默认 SHA-256，可选 BLAKE2b |
| A-02 | 概览和详细计划重复计算 MD5 | 大文件会产生双倍无必要 I/O | 单次扫描生成统计与计划 |
| A-03 | 扫描后直接移动，不复核文件 | 文件在确认期间变化时可能误移动 | 执行前复核大小、纳秒修改时间和完整哈希 |
| A-04 | 只在计划阶段检查目标名 | 扫描与执行之间可能出现同名目标 | 执行阶段再次选名，并用独占创建保留目标名 |
| A-05 | 使用 `shutil.move`，覆盖语义依平台和目标类型而异 | 可能产生不可预测的同名处理 | 同卷内独占占位后使用 `os.replace`，只替换本程序创建的空占位 |
| A-06 | 会跟随符号链接 | 可能读取或移动操作目录之外的文件 | 跳过文件链接和目录链接，拒绝链接根目录与输出目录 |
| A-07 | 月份正则接受 `2026-00`、`2026-99` | 扫描范围超出用户预期 | 月份严格限制为 `01` 至 `12` |
| A-08 | 系统目录只比较完全相等 | 可能允许 `C:\Windows\System32` 等子目录 | 禁止系统目录及其所有子目录 |
| A-09 | 排序在时间相同的情况下依赖枚举顺序 | 不同运行可能保留不同文件 | 使用规范路径作为最终稳定排序键 |
| A-10 | 执行结果缺少机器可读记录 | 难以追踪移动来源和目标 | 每次执行写入逐项 JSON Lines 日志 |
| A-11 | 移动后的竞态窗口没有复核 | 极小概率下可能移动并发变化的文件 | 移动后再次计算目标哈希并复核保留文件，失败时尝试安全回滚 |

## 当前判定算法

1. 只枚举合法月份目录；默认只读取其第一层常规文件。
2. 先按文件大小分组，单独大小的文件不做哈希。
3. 对同大小候选计算 SHA-256 或 BLAKE2b。
4. 只有“大小相同且完整哈希相同”才进入同一重复组。
5. 按配置的确定性规则选择一个保留文件，其余生成移动计划。
6. 计划中的目标路径位于“移动目的地/原月份/原相对路径”。同名时添加 `_1`、`_2` 等后缀。
7. 执行每项前重新验证保留文件和待移动文件；任一不符就停止后续操作。
8. 同卷操作使用原子替换；跨卷操作先完整复制、同步和哈希校验，目标落位后再次复核源文件，最后才删除源文件。
9. 移动后再次验证目标内容和保留文件；异常时保留源文件或尝试放回原路径，避免覆盖任何数据。

## 安全不变量

- 扫描函数不创建目录、不写日志、不移动文件。
- 不读取符号链接指向的内容。
- 不移动月份目录之外的源文件。
- 不把目标写到配置输出目录之外。
- 不覆盖扫描前已经存在或并发出现的用户文件。
- 扫描快照过期时不继续使用旧计划。
- 默认遇到第一项错误即停止，已成功项目有日志可追溯。

## 自动化验证

测试覆盖以下场景：

- 原始文件名与修改时间优先级。
- 同大小但不同内容不视为重复。
- 非法月份不进入扫描。
- 子目录默认忽略、显式开启后才扫描。
- 月内和跨月份两种范围。
- 已有目标文件不被覆盖。
- 扫描后文件变化导致计划失效。
- 移动后内容异常能够被发现并触发回滚。
- 成功移动生成机器可读日志。
- 输出文件夹名称安全检查。
- 真实 NTFS D→E 跨卷复制、校验和删除源文件流程。
- 系统允许创建链接时验证链接跳过；无创建权限时测试明确跳过。

## 剩余边界与使用建议

- 强哈希碰撞在工程上极难发生，但任何固定长度哈希都不是数学意义上的绝对唯一证明。
- 网络盘、云盘同步目录和特殊文件系统可能不支持标准的同卷原子替换；此时程序会停止并保留源文件。
- 其他程序持续改写或锁定文件时，该项目会失败并要求重新扫描，不会强行解锁。
- 操作日志用于审计，不等同于完整事务数据库；系统在极端断电时仍应依赖文件系统和备份恢复。
- 建议默认保持“不递归”和“仅月内比较”，只有明确理解目录结构时才扩大范围。
